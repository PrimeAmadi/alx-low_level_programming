/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  char str[] = "\"Programming is like building a multilingual puzzle";
  for(i = 0; str[i] ; i++)
    puts(str[i]);
  return (0);
}
